# FUNCTION-<<名称>> 说明

> 文件命名示例：`FUNCTION-CALC-PEAK.md`。请结合实际代码填写，确保读者可以从仓库快速定位实现并理解业务逻辑。

## 1. 功能概述
- **业务目标**：<<该函数/脚本解决的业务痛点或指标>>
- **核心入口**：`src/.../<<file.py>>::<function_name>>`
- **主要调用链**：<<列出关键调用者与被调用者，例如 `scheduler.run()` → `load_inputs()` → `FUNCTION`>>

## 2. 代码与业务流程
| 步骤 | 代码位置 | 业务说明 |
| --- | --- | --- |
| 1 | `src/.../<<file.py>>#LXX` | <<描述流水线第一步在业务中的作用>> |
| 2 | `src/.../<<file.py>>#LYY` | <<说明该步骤的计算或规则>> |
| … | … | … |

> 若存在多条分支流程，可追加小节分别说明，并标注触发条件。

### 关键代码片段
用 1-2 个片段展示核心实现，并配上简短注释说明上下文。优先引用真实业务代码，必要时可截取调用链或配置读取逻辑。

```python
# 来自 src/.../<<file.py>>#LXX-LYY
<<粘贴核心代码，并用行内注释解释关键步骤>>
```

> 若主体逻辑为文档/配置，可改用 ` ```yaml`、` ```sql` 等对应语言的代码块。

## 3. 输入、输出与副作用
| 项目 | 描述 |
| --- | --- |
| 输入 | <<参数、数据源、格式、校验规则>>
| 输出 | <<返回值、落地文件、下游接口>>
| 副作用 | <<日志、数据库写入、外部服务调用等>>

## 4. 配置与依赖
- **配置项**：<<涉及的配置文件或环境变量，及默认值>>
- **依赖模块/服务**：<<内部模块、外部 API、消息队列等>>
- **性能注意**：<<复杂度、并发限制、缓存策略等>>

## 5. 测试与验证
- **关联用例**：`tests/.../test_<<name>>.py`
- **覆盖重点**：<<列出关键场景，如边界值/失败重试/权限校验>>
- **手动验证**：<<可选，记录快速 smoke 方法或命令>>

## 6. 数据与风险
- **敏感信息处理**：<<如需脱敏或加密，说明方案>>
- **失败路径**：<<常见异常、回滚或补偿策略>>
- **已知限制**：<<例如仅支持特定地区/语言>>

## 7. 迭代记录
- **最近更新**：<<日期与简要说明>>
- **后续规划**：
  - [ ] <<下一步或优化方向>>
  - ❓ <<尚未确认的问题或外部依赖>>
